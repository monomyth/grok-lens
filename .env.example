# Optional environment overrides for local runs
# GROK_HOME=~/.grok
# HOST=127.0.0.1
# PORT=9292
# GROK_LENS_POLL_SECONDS=300
# Optional blended USD per 1M tokens for cost column (est. only):
# GROK_LENS_USD_PER_M_TOKENS=5.0
# Or put usd_per_m_tokens in ~/.grok-lens.yml
